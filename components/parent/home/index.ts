// components/parent/home/index.ts
// Barrel exports for parent home components
// Note: StatCard, MobileStatCard, QuickAction, SectionHeader are reused from helper/home

export { Sidebar } from './Sidebar';

// These components are imported from helper/home (already created):
// - MobileHeader (same)
// - StatCard (same)
// - MobileStatCard (same)
// - QuickAction (same)
// - SectionHeader (same)
// - GreetingCard (just change color)
